apiVersion: v1alpha1
kind: Setting
metadata:
  name: setting-plugin-bytemd
spec:
  forms:
    - group: "basic"
      label: "基础设置"
      formSchema:
        - $formkit: select
          name: keymap
          label: "键映射"
          value: "default"
          options:
            - label: "默认"
              value: "default"
            - label: "Vim"
              value: "vim"
